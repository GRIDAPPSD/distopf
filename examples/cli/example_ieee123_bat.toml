# Minimal user-authored DistOPF scenario.
# Paths are resolved relative to this file.

[case]
path = "../../src/distopf/cases/csv/ieee123_bat"
n_steps = 24
start_step = 0

[analysis]
type = "opf"
objective = "loss"

# Optional: save results and the generated run_config.json here.
[output]
directory = "../../scratch/example_results_ieee123_bat"

